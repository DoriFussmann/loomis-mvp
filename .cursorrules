# Project Scaffold — Cursor Rules
# Last updated: auto-maintained. If your change affects architecture, stack, auth, data,
# routing, env vars, or any public-facing interface → update HANDOFF.md immediately.

## ⚠️ HANDOFF.md is a living document
Whenever you make a change that affects any of the following, you MUST update HANDOFF.md:
- Project structure or file locations
- Auth logic or session handling
- Data shape (users.json, pages.json, prompts.json)
- Environment variables
- API routes (adding, removing, changing method/path)
- Admin UI capabilities
- Page routing conventions
- Deployment or build process
- Any new dependency added to package.json

Do not skip this. HANDOFF.md is the source of truth for any engineer picking this up.

---

## Stack
- Next.js 14, App Router
- TypeScript
- Tailwind CSS (utility-first, no component libraries)
- Flat JSON files for persistence (/data/*.json)
- JWT in httpOnly cookies for auth
- Anthropic SDK for AI (key in .env.local)

---

## Design System

### Typography
- Font: Inter (`var(--font-inter)`), fallbacks `system-ui`, `sans-serif`
- Loaded weight: `400` only
- Global: `font-weight: 400 !important` on `*` — no bold. `letter-spacing: -0.01em`. Antialiased.
- Base size: `13px` on `body` (`text-[13px]`)
- Page title: `text-[15px] text-ink`
- Card title / body: `13px` / `400` / ink
- Muted metadata: `text-muted-foreground` (`#8a8a8a`)

### Color Tokens
Named hex tokens in `tailwind.config.ts` plus CSS variables in `globals.css`.

| Token | Hex | Role |
|---|---|---|
| `blueprint` | `#2c4a6e` | Login left panel |
| `white` | `#ffffff` | Body + card surfaces |
| `line` | `#e6e6e6` | Borders, dividers |
| `ink` | `#1a1a1a` | Primary text |
| `muted` / `--muted-foreground` | `#8a8a8a` | Secondary text |
| `soft` | `#fafafa` | Soft fills, hover, active nav |
| `accent` / `--primary` | `#2f5eff` | Status complete, links, errors |
| `check` | `#6a9a78` | Confirmed checkmarks |
| `placeholder` | `#b5b5b5` | Input placeholders |
| `scrollbar` | `#e0e0e0` | Thin scrollbar thumb |

Login panel copy: `text-white/50` (eyebrow), `text-white/70` (subcopy), `text-white/40` (footer).

### Border Radius
- `lg`: `0.375rem` (6px) — nav items, inputs, buttons
- `xl`: `0.5rem` (8px) — cards
- `2xl`: `0.75rem` (12px)

### Layout Rules
- Max content width: 1280px (`max-w-[1280px]`) on ALL pages and layouts
- Always center constrained content with `mx-auto`
- Global navigation: `h-12`, `border-b border-line` — `components/site-header.tsx` (user) and `app/admin/layout.tsx` (admin)
- User-facing pages live in `app/(app)/` — they inherit `SiteHeader` + `px-8 py-6`
- Never add per-page outer wrappers or padding when inside the `(app)/` route group
- Content column padding: `px-8 py-6`
- Left nav: `w-52`, `border-r`, `p-3`, `gap-3` bordered items
- Login: 50/50 split; card `max-w-[420px] rounded-xl border-line p-6`

### Aesthetic Rules
- White surfaces, 1px `#e6e6e6` borders, **no shadows**
- Ghost outlined buttons: `rounded-lg border border-line px-3 py-2 text-muted-foreground hover:text-ink disabled:opacity-40` — no filled primary
- Cards: `bg-white border-line rounded-xl`, header/body `px-4 py-3`, hover `bg-soft`
- Nav item default: `border-line text-muted-foreground hover:bg-soft hover:text-ink`
- Nav item active: `border-line bg-soft text-ink`
- Inputs: `rounded-lg border border-line px-3 py-2`, outline none, placeholder `#b5b5b5`
- Voice: minimal labels. Placeholders and button text only where possible.
- Icons: Lucide React, `w-4 h-4` standard
- Keep the Loomis logo on login (blueprint panel + mobile) and in the site/admin headers

---

## File & Folder Conventions
- /app — Next.js App Router pages and layouts
- /app/admin/* — Admin-only routes (middleware-protected)
- /app/(auth)/login — Login page
- /app/test — Demo user-facing page
- /components/ui — Reusable UI primitives
- /components/admin — Admin-specific components
- /data/*.json — Flat file persistence (committed to repo)
- /lib/auth.ts — JWT creation, verification, session helpers
- /lib/data.ts — Read/write helpers for JSON data files
- /lib/prompts.ts — Prompt resolution and variable interpolation

---

## Auth Rules
- Admin: role "admin" in users.json
- Users: role "user", access scoped to allowedPages[] in their record
- JWT stored in httpOnly cookie named "session"
- Middleware at /middleware.ts protects /admin/* and scoped user pages
- JWT_SECRET lives in env only, never in source

---

## Data Rules
- Never hardcode prompts, users, or page configs in source code
- All prompts live in /data/prompts.json, edited only via Admin UI
- All page definitions in /data/pages.json
- All users in /data/users.json
- Writes go through /lib/data.ts — never fs directly in route handlers
- Data files are committed and deployed — they ARE the database

---

## AI / Prompt Rules
- ANTHROPIC_API_KEY in .env.local only, never committed
- Prompts fetched at runtime from prompts.json, never hardcoded
- Variables use {{variableName}} syntax
- Interpolation happens server-side in /lib/prompts.ts
- Users never see raw prompt text

---

## API Route Conventions
- All routes under /app/api/
- Auth: /api/auth/login, /api/auth/logout
- Admin: /api/admin/* (middleware-protected)
- AI execution: /api/run-prompt
- Response shape: { success: boolean, data?, error? }

---

## Code Style
- TypeScript strict mode, no any types
- Functional components only
- Server Components by default; "use client" only when needed
- Named exports for components, default exports for pages
- Keep route handlers thin — logic in /lib
